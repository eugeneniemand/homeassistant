alias: Office Motion Turn On Lights
initial_state: true
trigger:
- platform: mqtt
  topic: 'zigbee2mqtt/office_motion'
condition:
  condition: and 
  conditions:
  - condition: template
    value_template: "{{ trigger.payload_json.occupancy == true }}"
  - condition: or
    conditions:     
      - condition: template
        value_template: "{{states('sensor.office_lux') | int <= states('input_number.office_lux_limit') | int}}"
      - condition: state
        entity_id: light.office
        state: 'on'
action:
- service: timer.cancel
  data:
    entity_id: timer.office
- service: timer.start
  data_template:
    entity_id: timer.office
    duration: 00:{{ '{:02}'.format(states('input_number.office_timeout') | int) }}:00
- condition: state
  entity_id: light.office
  state: 'off'
- service: light.turn_on
  data_template:
    entity_id: light.office
    kelvin: "{{ state_attr('sensor.circadian_values', 'colortemp') | int }}"
    brightness_pct: "{{ state_attr('switch.circadian_lighting_circadian_lighting', 'brightness') | int }}"